apiVersion: v1
kind: Secret
metadata:
  name: kith-secrets
  namespace: kith-pms
  annotations:
    # This file is a placeholder. Apply real secrets out-of-band:
    #   kubectl create secret generic kith-secrets -n kith-pms \
    #     --from-literal=SESSION_SECRET=<32+ random bytes> \
    #     --from-literal=APP_PASSWORD_HASH=<bcrypt hash>
    # Consider SealedSecrets or external-secrets-operator for GitOps workflows.
type: Opaque
stringData:
  SESSION_SECRET: "REPLACE_ME_openssl_rand_hex_32"
  APP_PASSWORD_HASH: "REPLACE_ME_bcrypt_hash"
  TOKEN_AUTH: ""
